server:
  host: "0.0.0.0"
  port: 8080
  mode: "release"

database:
  driver: "sqlite"
  dsn: "/var/lib/frp-panel/frp-panel.db"

redis:
  addr: "localhost:6379"
  password: ""
  db: 0

jwt:
  secret: "CHANGE_ME_TO_A_LONG_RANDOM_SECRET"
  expire_time: 24h
  issuer: "frp-panel"

frp:
  default_version: "0.68.0"
  github_mirror: ""
  download_timeout: 300
  plugin_webhook_url: ""
  poller_interval: 30
  server_token: "CHANGE_ME_TO_A_LONG_RANDOM_TOKEN"

admin:
  email: "admin@example.com"
  password: "CHANGE_ME_IMMEDIATELY"

update:
  center_url: "https://08642.xyz"
  instance_key: ""
  instance_id: ""
  panel_version: "dev"
  panel_domain: ""
  heartbeat_enabled: false
  heartbeat_interval: 5s
  control_public_key: "HU5iQjEL7v24v5aK0K+gHTctvWorW+iLq5bhpXf9lSU="
  identity_key_file: "/var/lib/frp-panel/update-identity.key"
  lease_cache_file: "/var/lib/frp-panel/update-lease.json"
  bootstrap_cache_file: "/var/lib/frp-panel/update-bootstrap.json"
  bootstrap_urls: []
  anonymous_statistics: false # Deprecated alias for heartbeat_enabled.
